# Artifacts

This directory holds the staged static release artifact used by the Quadlet
container build.

Expected file:

```text
deployment/artifacts/nkl-stack-playground
```

Prepare it with:

```bash
./deployment/prepare-quadlet-release.sh
```
